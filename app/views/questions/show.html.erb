<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>

<br/>

<p><%= @question.body %></p>

Resolved
<%= check_box_tag "resolved", "yes", @question.resolved, disabled: true %>
